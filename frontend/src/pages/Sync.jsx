import React from 'react';
import SyncButton from '../components/SyncButton';

const Sync = () => (
  <div>
    <h1 style={{ fontFamily: "'Lora', serif", fontSize: '2.25rem', fontWeight: 'bold', marginBottom: '1rem' }}>Синхронізація даних</h1>
    <SyncButton />
  </div>
);

export default Sync; 